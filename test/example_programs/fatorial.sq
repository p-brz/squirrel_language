int fatorial(float n)
{
    int x=1;
    print("local x: ",x);
    if(n==1) {
        return n;
    }
    else
    {
         return (fatorial(n-1) * n);     
    }
}

int x=10;

int main ()
{
    print("global x: ", x);
    //int n = int(readline());
    boolean n = true;
    int fat = fatorial(n);
    print("Fatorial de ",n, "e: ", fat);
    
    print("global x: ", x);
	return 0;
}